name: sertifikasi_jmp_final
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  sensors_plus: ^6.0.0
  flutter_secure_storage: ^9.2.2
  google_maps_flutter: ^2.9.0
  http: ^1.2.2  

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
